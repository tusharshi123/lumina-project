# Stellar Network Configuration
NEXT_PUBLIC_STELLAR_NETWORK=testnet

# Smart Contract Address (get after deploying contract to testnet)
# Deploy contract with: soroban contract deploy --network testnet --wasm ./contract/target/wasm32-unknown-unknown/release/lumina_contract.wasm
NEXT_PUBLIC_CONTRACT_ADDRESS=CXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Wallet Configuration
NEXT_PUBLIC_WALLET_NETWORK=testnet

# Optional: API endpoints for Horizon (if needed for advanced features)
# NEXT_PUBLIC_STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
